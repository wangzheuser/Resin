/resin
/resin.exe
/resin.exe~
sing-box-reference
.agents
.devcontainer
start-instance.sh

# Environment variables (contains secrets)
.env

# Claude Code / OMC tooling and local runtime data
.claude/
.omc/
.local/
.setting/
.rollout/
.spec-workflow/
data/
AGENTS.md

# WorkBuddy AI / Codex local runtime data
.workbuddy-ai/
.codex-artifacts/

# Local machine-specific compose override (points at host restore volumes)
docker-compose.override.yml
